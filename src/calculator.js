export const Addition = (num1, num2) => {
  return num1 + num2;
};

export const Subtract = (num1, num2) => {
  return num1 - num2;
};

export const Multiplay = (num1, num2) => {
  return num1 * num2;
};

export const Division = (num1, num2) => {
  return num1 / num2;
};
